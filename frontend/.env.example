# Optional — only needed if NOT using the Vite dev proxy (i.e. standalone frontend build)
# In local dev the proxy in vite.config.js handles /api → localhost:5001 automatically
# Set this for a standalone deployment pointing at a remote backend
VITE_API_URL=https://your-backend-url.up.railway.app
